import React from 'react';

function CIS_ques(props) {
    return (
        <div>
            <div>
                <h6>{props.question}</h6>
            </div>
            {/* <div>
                <input type={props.type} value={props.value} name={props.name} /> {props.OptionName}
            </div> */}
        </div>
    );
}

export default CIS_ques;